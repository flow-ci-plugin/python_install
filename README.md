
# python_install Step
Install dependencies use `pip install -r requirements.txt`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: python_install
    enable: true
    failure: true
    plugin:
      name: python_install
      inputs:
```
